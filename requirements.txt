fastapi==0.116.1
httpx==0.28.1
uvicorn==0.35.0
# 91crdj publishes its artwork as AES-CBC blobs, so the poster proxy needs a
# real AES backend in the image; without it every drama poster returns 502.
cryptography==50.0.1
# The poster proxy downsamples artwork to the width the client actually asks
# for and re-encodes it as WebP. That is what keeps the movie wall and the
# ASMR covers from shipping multi-megabyte originals over a slow link.
Pillow==12.3.0
